
fetch call to create a new playlist:

    fetch("/playlist/create", {
          method: "POST",
          body: JSON.stringify({
            name: "abcd",
            description: "abcde"
          }),
          headers: {
            "content-type": "application/json",
          },
        })

server call for searching new song:

        const onSearch = () => {
    fetch("playlist/search", {
      method: "POST",
      body: JSON.stringify({
        searchQuery: "cheap thrills",
      }),
      headers: {
        "content-type": "application/json",
      },
    })
  }

server call for adding new song:
  fetch("/playlist/:id", {
      method: "POST",
      body: JSON.stringify({
        playlistId: 23,
        track: ["spotify:track:27SdWb2rFzO6GWiYDBTD9j"]
      }),
      headers: {
        "content-type": "application/json",
      },
    })